<% @contacts.each do |contact| %>
	<div class="homepage_contact">
	  <figure class="contact_photo homepage" href="/contacts/<%= contact.id %>"></figure>
	  <a class="contact_name" id="<%= contact.id %>" href="/contacts/<%= contact.id %>"><%= contact.name %></a>
	</div>
<% end %>